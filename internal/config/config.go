// Package config loads ~/.mindskein/config.toml: vault path, plan.md path and
// project roots.
//
// Implemented by U3 (priorities parser), which is the first unit that needs to
// find the vault.
package config
